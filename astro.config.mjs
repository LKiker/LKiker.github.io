// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://lkiker.github.io',
  // no `base` needed for a username.github.io repo
});
